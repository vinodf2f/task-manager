const initialState = {
  tasks: [],
  isloading: false,
  error: false,
};

const actions = {
  ADD_TASK: "ADD_TASK",
  REMOVE_TASK: "REMOVE_TASK",
  FETCH_TASKS: "FETCH_TASKS",
};

const reducer = (state, action) => {
  switch (action.type) {
    case actions.ADD_TASK:
      break;

    default:
      break;
  }
};
